# ".gitignore" is a file used by Git to specify intentionally untracked files and directories that Git should ignore, preventing them from being staged or committed.

git add .gitignore

git commit -m <"To ignore files">