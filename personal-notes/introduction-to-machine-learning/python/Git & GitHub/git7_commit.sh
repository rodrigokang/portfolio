# Record changes made to the repository. It creates a snapshot of the changes that can be referenced in the future.

# For a single file
git commit -m <"file_description">

# For a multiple files

git commit -m <"files_description">